[project]
name = "dubai-news-rt"
version = "0.1.0"
description = "Dubai News Aggregator Backend"
dependencies = [
    "fastapi",
    "google-generativeai",
    "requests",
    "pydantic",
    "python-dotenv"
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk"
]